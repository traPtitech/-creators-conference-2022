mixin group(group)
  .group-wrapper
    .group
      h3.name= group.name
      p.desc= group.desc
      button.toggle-details もっと詳しく
      .details.hidden
        .inner-wrapper
          button.toggle-details 閉じる
          each line in group.details.desc
            p !{line}
