mixin group(group)
  .group-wrapper
    .group
      h3.name= group.name
      p.desc= group.desc
      button.toggle-details もっと詳しく
      .details.hidden
        .inner-wrapper
          each line in group.details.desc
            p !{line}
          each img in group.details.img
            img(src=img loading="lazy")
          .slides
            each img in group.details.slide
              img(src=img loading="lazy")
          button.toggle-details 閉じる
